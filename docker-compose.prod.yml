services:
  postgres:
    image: postgres:16-alpine
    container_name: talimcrm_postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be defined}
      POSTGRES_DB: ${POSTGRES_DB:-talimcrm}
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    command: >
      postgres
      -c shared_buffers=256MB
      -c max_connections=200
      -c work_mem=16MB
      -c maintenance_work_mem=64MB
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-talimcrm}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - talimcrm_net

  redis:
    image: redis:7-alpine
    container_name: talimcrm_redis
    restart: always
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD:-talimcrmredisprodpass}"]
    volumes:
      - redis_prod_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-talimcrmredisprodpass}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - talimcrm_net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runner
    container_name: talimcrm_backend
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-talimcrm}
      REDIS_URL: redis://:${REDIS_PASSWORD:-talimcrmredisprodpass}@redis:6379
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be defined}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      PORT: 4000
      FRONTEND_URL: ${FRONTEND_URL:-https://crmapp.uz}
      # Optional external integrations
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_BOT_USERNAME: ${TELEGRAM_BOT_USERNAME:-}
      CLICK_MERCHANT_ID: ${CLICK_MERCHANT_ID:-}
      CLICK_SERVICE_ID: ${CLICK_SERVICE_ID:-}
      CLICK_SECRET_KEY: ${CLICK_SECRET_KEY:-}
      PAYME_MERCHANT_ID: ${PAYME_MERCHANT_ID:-}
      PAYME_KEY: ${PAYME_KEY:-}
      PLATFORM_CLICK_MERCHANT_ID: ${PLATFORM_CLICK_MERCHANT_ID:-}
      PLATFORM_CLICK_SERVICE_ID: ${PLATFORM_CLICK_SERVICE_ID:-}
      PLATFORM_CLICK_SECRET_KEY: ${PLATFORM_CLICK_SECRET_KEY:-}
      PLATFORM_PAYME_MERCHANT_ID: ${PLATFORM_PAYME_MERCHANT_ID:-}
      PLATFORM_PAYME_KEY: ${PLATFORM_PAYME_KEY:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      SMTP_FROM: ${SMTP_FROM:-TalimCRM <no-reply@crmapp.uz>}
      SENTRY_DSN: ${SENTRY_DSN:-}
    volumes:
      - uploads_prod_data:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:4000/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - talimcrm_net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: runner
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
    container_name: talimcrm_frontend
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: "0.0.0.0"
    networks:
      - talimcrm_net

  nginx:
    image: nginx:1.27-alpine
    container_name: talimcrm_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
      - uploads_prod_data:/app/uploads:ro
    depends_on:
      - frontend
      - backend
    networks:
      - talimcrm_net

  certbot:
    image: certbot/certbot:latest
    container_name: talimcrm_certbot
    restart: unless-stopped
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  db-backup:
    image: postgres:16-alpine
    container_name: talimcrm_backup
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-talimcrm}
      BACKUP_KEEP_DAYS: ${BACKUP_KEEP_DAYS:-14}
    volumes:
      - ./backups:/backups
    entrypoint: >
      /bin/sh -c '
      echo "TalimCRM avtomatik zaxira nusxalash xizmati ishga tushdi (Har kuni soat 03:00 da)...";
      mkdir -p /backups;
      while true; do
        NOW_HOUR_MIN=$$(date +%H:%M);
        if [ "$$NOW_HOUR_MIN" = "03:00" ]; then
          TIMESTAMP=$$(date +%Y-%m-%d_%H%M%S);
          TARGET="/backups/talimcrm_$${TIMESTAMP}.sql.gz";
          echo "[$$(date)] Zaxira nusxa olinmoqda: $$TARGET";
          PGPASSWORD="$$POSTGRES_PASSWORD" pg_dump -h "$$POSTGRES_HOST" -U "$$POSTGRES_USER" "$$POSTGRES_DB" | gzip > "$$TARGET";
          echo "[$$(date)] Eski zaxiralar tozalanmoqda (> $$BACKUP_KEEP_DAYS kun)...";
          find /backups -name "talimcrm_*.sql.gz" -type f -mtime +$$BACKUP_KEEP_DAYS -delete;
          echo "[$$(date)] Zaxira muvaffaqiyatli yakunlandi.";
          sleep 60;
        fi;
        sleep 30;
      done;
      '
    networks:
      - talimcrm_net

networks:
  talimcrm_net:
    driver: bridge

volumes:
  postgres_prod_data:
  redis_prod_data:
  uploads_prod_data:
  certbot_conf:
  certbot_www:
